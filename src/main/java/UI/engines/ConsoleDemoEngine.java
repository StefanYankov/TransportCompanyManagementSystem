package UI.engines;

import UI.controllers.DriverController;
import UI.controllers.TransportCompanyController;

public class ConsoleDemoEngine {

//    TransportCompanyController companyController = new TransportCompanyController(validator, transportCompanyService);
//    DriverController driverController = new DriverController(validator, driverService, qualificationService, transportCompanyService);
//
//    // Test ui for company controller
//        logger.debug("{}{}Company controller starts{}{}", "-".repeat(12), System.lineSeparator(), System.lineSeparator(), "-".repeat(12));
//        companyController.run();
//        logger.debug("{}{}Company controller ends{}{}", "-".repeat(12), System.lineSeparator(), System.lineSeparator(), "-".repeat(12));
//
//
//    // Test ui for company controller
//        logger.debug("{}{}Driver controller starts{}{}", "-".repeat(12), System.lineSeparator(), System.lineSeparator(), "-".repeat(12));
//        driverController.run();
//        logger.debug("{}{}Driver controller ends{}{}", "-".repeat(12), System.lineSeparator(), System.lineSeparator(), "-".repeat(12));

}
